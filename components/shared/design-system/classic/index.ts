/** Classic MyOffice (Studio glass/glow). Implementation stays in the parent
 *  tokens / primitives / components — those files are the Classic system.
 *  Dallaglio lives in ../dallaglio and must not be layered onto these. */
export type ClassicDesign = 'classic';
